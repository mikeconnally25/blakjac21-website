/* Deprecated external loader — Stake CSP blocks this file when injected on stake.com.
   Use Bonus Hunt → Sync slots → Copy script, then paste into the stake.com console. */
(function () {
  window.alert(
    "This old BJ21 sync bookmarklet no longer works on stake.com.\n\n" +
      "Go to Bonus Hunt → Sync slots from Stake → Copy script → paste into the stake.com console (F12)."
  );
})();
